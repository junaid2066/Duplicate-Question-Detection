# Contributing

1. Fork the repository and create a feature branch.
2. Do not commit datasets, model checkpoints, API keys, or access tokens.
3. Keep reusable code inside `src/` and experiments inside `notebooks/`.
4. Clear large notebook outputs before committing.
5. Open a pull request with a clear description of the change and its results.
